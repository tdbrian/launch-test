import 'socket.io';
// import { attemptLogin } from './app/login/login.api';
// import { ATTEMPT_LOGIN } from './app/shared/services/socketio.constants';

export default (socket: SocketIO.Socket) => {
    // socket.on(ATTEMPT_LOGIN, attemptLogin);
};
